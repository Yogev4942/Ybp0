<UserControl x:Class="ybp0.Views.CreatePostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ybp0.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Input Field Style -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="#AAFFFFFF"/>
            <Setter Property="BorderBrush" Value="#44009688"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalScrollBarVisibility="Auto"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#009688"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#00695C"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E8F5E9" Offset="0"/>
                <GradientStop Color="#E0F2F1" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Glassy Create Post Card -->
        <Border Grid.Row="1"
                Width="500"
                Padding="40,35"
                CornerRadius="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Background>
                <SolidColorBrush Color="#88FFFFFF"/>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#26A69A" BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Disabled" >
                <StackPanel>
                    <!-- Title -->
                    <TextBlock Text="Create Post"
                           Foreground="#004D40"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8" />

                    <TextBlock Text="Share your thoughts and achievements with our community"
                           Foreground="#00695C"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                    <!-- Error/Success Message -->
                    <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{Binding MessageColor}"
                           FontSize="13"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"
                           TextWrapping="Wrap"/>

                    <!-- Header Input -->
                    <StackPanel Margin="0,0,0,18">
                        <TextBlock Text="Header" Style="{StaticResource LabelStyle}"/>
                        <TextBox Height="45"
                             Text="{Binding Header, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource InputTextBoxStyle}"
                             MaxLength="100"
                             VerticalContentAlignment="Center"/>
                        <TextBlock Text="{Binding HeaderCharCount}" 
                               Foreground="#00695C" 
                               FontSize="11" 
                               HorizontalAlignment="Right"
                               Margin="0,3,0,0"
                               Opacity="0.7"/>
                    </StackPanel>

                    <!-- Content Input -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Content" Style="{StaticResource LabelStyle}"/>
                        <TextBox Height="150"
                             Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource InputTextBoxStyle}"
                             MaxLength="500"
                             VerticalScrollBarVisibility="Auto"/>
                        <TextBlock Text="{Binding ContentCharCount}" 
                               Foreground="#00695C" 
                               FontSize="11" 
                               HorizontalAlignment="Right"
                               Margin="0,3,0,0"
                               Opacity="0.7"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Cancel Button -->
                        <Button Grid.Column="0"
                            Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Height="45"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            BorderThickness="1"
                            BorderBrush="#26A69A"
                            Background="Transparent" 
                            Foreground="#26A69A">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#1126A69A"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Post Button -->
                        <Button Grid.Column="2"
                            Content="Post"
                            Command="{Binding CreatePostCommand}"
                            Height="45"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            BorderThickness="0"
                            Background="#26A69A" 
                            Foreground="White">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="10">
                                        <Border.Effect>
                                            <DropShadowEffect x:Name="glow" 
                                                        Color="#26A69A" 
                                                        BlurRadius="0" 
                                                        ShadowDepth="0" 
                                                        Opacity="0"/>
                                        </Border.Effect>
                                        <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="glow"
                                                                   Storyboard.TargetProperty="BlurRadius"
                                                                   To="10" Duration="0:0:0.2"/>
                                                        <DoubleAnimation Storyboard.TargetName="glow"
                                                                   Storyboard.TargetProperty="Opacity"
                                                                   To="0.4" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="glow"
                                                                   Storyboard.TargetProperty="BlurRadius"
                                                                   To="0" Duration="0:0:0.2"/>
                                                        <DoubleAnimation Storyboard.TargetName="glow"
                                                                   Storyboard.TargetProperty="Opacity"
                                                                   To="0" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Background" Value="#AAAAAA"/>
                                            <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
