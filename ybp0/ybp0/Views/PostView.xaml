<UserControl x:Class="ybp0.Views.PostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ybp0.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10, 8">
        <!-- Main Card Container -->
        <Border CornerRadius="12"
                BorderThickness="1"
                BorderBrush="#40FFFFFF">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#CCFFFFFF" Offset="0"/>
                    <GradientStop Color="#99F0F8FF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#00B0FF" 
                                  BlurRadius="15" 
                                  ShadowDepth="2" 
                                  Opacity="0.15"/>
            </Border.Effect>

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="*"/>    <!-- Content -->
                    <RowDefinition Height="Auto"/> <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- Glossy Overlay -->
                <!-- <Border Grid.RowSpan="3" CornerRadius="12" Background="{x:Null}" IsHitTestVisible="False">
                       You can add a linear gradient here for extra gloss if needed, but keeping it simple for now
                </Border> -->

                <!-- Header: Avatar + Name + Time -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Avatar (Clickable) -->
                    <Button Command="{Binding NavigateToProfileCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="0"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Width="40" Height="40" CornerRadius="20" Background="{Binding AvatarColor, Converter={StaticResource StringToColorBrushConverter}}" 
                                        BorderBrush="White" BorderThickness="2">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="1" Opacity="0.2"/>
                                    </Border.Effect>
                                    <TextBlock Text="{Binding Initials}" 
                                               VerticalAlignment="Center" 
                                               HorizontalAlignment="Center" 
                                               Foreground="White" 
                                               FontWeight="Bold"
                                               FontSize="14"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- User Info -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Username}" 
                                   FontWeight="Bold" 
                                   FontSize="14" 
                                   Foreground="#37474F"/>
                        <TextBlock Text="{Binding Timestamp}" 
                                   FontSize="11" 
                                   Foreground="#78909C"/>
                    </StackPanel>
                    
                    <!-- Menu/More Icon (Visual Placeholder) -->
                    <TextBlock Grid.Column="2" Text="..." FontSize="20" Foreground="#90A4AE" VerticalAlignment="Top" Margin="0,-8,0,0" Cursor="Hand"/>
                </Grid>

                <!-- Content -->
                <TextBlock Grid.Row="1" 
                           Text="{Binding Content}" 
                           TextWrapping="Wrap" 
                           FontSize="14" 
                           Foreground="#263238" 
                           LineHeight="20"
                           Margin="0,5,0,15"/>

                <!-- Footer: Actions -->
                <!-- Ideally these would be buttons with Commands, but purely visual for now -->
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <!-- Like -->
                    <Border CornerRadius="12" Background="#2000B0FF" Padding="12,5" Margin="0,0,10,0" Cursor="Hand">
                         <TextBlock Text="❤️ Like" Foreground="#0277BD" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                    
                    <!-- Comment -->
                    <Border CornerRadius="12" Background="#10000000" Padding="12,5" Margin="0,0,10,0" Cursor="Hand">
                         <TextBlock Text="💬 Comment" Foreground="#546E7A" FontSize="12" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
