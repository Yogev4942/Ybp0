<Window x:Class="ybp0.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ybp0"
        xmlns:vm="clr-namespace:ViewModels.ViewModels;assembly=ViewModels"
        Title="MainWindow" Height="600" Width="800" WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <!--WindowStyle="None">--> 
    

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#44FFFFFF" Offset="0"/>
                        <GradientStop Color="#22FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#77FFFFFF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect x:Name="shadow" Color="#00FFCC" BlurRadius="5" ShadowDepth="0" Opacity="0.2"/>
                            </Border.Effect>
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}">
                                    <ContentPresenter.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1" Opacity="0.5"/>
                                    </ContentPresenter.Effect>
                                </ContentPresenter>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" 
                                                          Storyboard.TargetProperty="(Border.Background).(LinearGradientBrush.GradientStops)[0].Color"
                                                          To="#66FFFFFF" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="shadow"
                                                           Storyboard.TargetProperty="BlurRadius"
                                                           To="20" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="shadow"
                                                           Storyboard.TargetProperty="Opacity"
                                                           To="0.9" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                          Storyboard.TargetProperty="(Border.Background).(LinearGradientBrush.GradientStops)[0].Color"
                                                          To="#44FFFFFF" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="shadow"
                                                           Storyboard.TargetProperty="BlurRadius"
                                                           To="5" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="shadow"
                                                           Storyboard.TargetProperty="Opacity"
                                                           To="0.2" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#88FFFFFF" Offset="0"/>
                                            <GradientStop Color="#44FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Background with subtle animated gradient -->
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#004D40" Offset="0"/>
                <GradientStop Color="#00897B" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                        <ColorAnimation Storyboard.TargetProperty="(Grid.Background).(LinearGradientBrush.GradientStops)[1].Color"
                                        To="#26A69A" Duration="0:0:8"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <DockPanel>
            <!-- Clean Top Navigation Bar -->
            <Border DockPanel.Dock="Top" 
                    Height="70"
                    Margin="0"
                    Padding="20,15"
                    Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#22FFFFFF" Offset="0"/>
                        <GradientStop Color="#11FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect Color="#00FFCC" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>

                <Grid>
                    <!-- Centered Navigation Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Home Button -->
                        <Button Content="🏠 Home" 
                                Command="{Binding NavigateCommand}" 
                                CommandParameter="{x:Type vm:HomeViewModel}"
                                Style="{StaticResource NavButtonStyle}"/>
                        
                        <!-- Calendar Button -->
                        <Button Content="📅 Calendar" 
                                Command="{Binding NavigateCommand}" 
                                CommandParameter="{x:Type vm:CalendarViewModel}"
                                Style="{StaticResource NavButtonStyle}"/>

                        <Button Content="🏋 Workout"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Type vm:WorkoutViewModel}"
                                Style="{StaticResource NavButtonStyle}"/>
                        
                        <!-- Feed Button -->
                        <Button Content="📰 Feed" 
                                Command="{Binding NavigateCommand}" 
                                CommandParameter="Feed"
                                Style="{StaticResource NavButtonStyle}"/>

                        <!-- Search Button -->
                        <Button Content="🔍 Search" 
                                Command="{Binding NavigateCommand}" 
                                CommandParameter="{x:Type vm:SearchViewModel}"
                                Style="{StaticResource NavButtonStyle}"/>
                    </StackPanel>

                    <!-- User Profile (Right Aligned) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,-10">
                        <Button Background="Transparent" BorderThickness="0" Cursor="Hand" 
                                Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Type vm:ProfileViewModel}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent" CornerRadius="20" Padding="8,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#22FFFFFF"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <!-- Avatar Circle -->
                                            <Border Width="40" Height="40" CornerRadius="18" Background="{Binding AvatarColor, Converter={StaticResource StringToColorBrushConverter}}" 
                                                    BorderBrush="White" BorderThickness="1.5" Margin="0,0,10,0">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                                </Border.Effect>
                                                <TextBlock Text="{Binding Initials}" 
                                                           Foreground="White" 
                                                           FontWeight="Bold" 
                                                           FontSize="12"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                            <!-- Username -->
                                            <TextBlock Text="{Binding Username}" 
                                                       Foreground="White" 
                                                       FontWeight="SemiBold" 
                                                       FontSize="14"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>

                    <!-- Logout Button (Far Right) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="🚪 Logout" 
                                Command="{Binding LogoutCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <ContentControl Content="{Binding CurrentViewModel}" 
                          Margin="0"/>
        </DockPanel>
    </Grid>

</Window>
